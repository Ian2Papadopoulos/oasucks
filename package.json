{
  "name": "oasa-stop",
  "version": "1.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "Live OASA arrivals — Cloudflare Worker serving the app + API proxy.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/tiles.mjs && node test/brand.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.117.0",
    "playwright-core": "^1.49.0"
  }
}
